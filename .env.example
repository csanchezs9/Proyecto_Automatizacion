# Configuración del servidor
PORT=3001
NODE_ENV=development

# Configuración de la base de datos PostgreSQL
DB_HOST="your-supabase-host.supabase.co"
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres.your-project-ref
DB_PASSWORD=YOUR_SECURE_PASSWORD_HERE

# Configuración de URLs de Supabase
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Configuración específica para Supabase (para scripts directos)
SUPABASE_HOST=your-supabase-host.supabase.co
SUPABASE_PORT=5432
SUPABASE_DB=postgres
SUPABASE_USER=postgres.your-project-ref
SUPABASE_PASSWORD=YOUR_SECURE_PASSWORD_HERE

# =============================================================================
# TEMPLATE FILE - NO REAL CREDENTIALS
# =============================================================================
# This is a template file with placeholder values only.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file as .env: cp .env.example .env
# 2. Replace ALL placeholder values with your real Supabase credentials
# 3. NEVER commit the .env file to version control
# 4. The .env file is already listed in .gitignore
#
# WARNING: These are PLACEHOLDER values, not real credentials!